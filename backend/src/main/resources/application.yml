
management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true

net:
  wohlfart:
    charon:
      data-dir: "test"
      webjar-base: "/charon"
