#
# some protected data from the backend
#
type: object
required:
  - value
  - expire
properties:
  value:
    example: "sresegrsg"
    type: string
  expire:
    example: "1995-09-07T10:40:52Z"
    format: date-time
    type: string

