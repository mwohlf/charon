apiVersion: v1
kind: Service
metadata:
  name: public-aouth-svc
spec:
  type: LoadBalancer
  selector:
    app: charon-aouth
  ports:
    - port: 81
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: charon-aouth-deployment
spec:
  selector:
    matchLabels:
      app: charon-aouth
  replicas: 1
  template:
    metadata:
      labels:
        app: charon-aouth
    spec:
      containers:
        - name: charon-aouth
          image: ttl.sh/mwohlf/charon-oauth
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            initialDelaySeconds: 15
            periodSeconds: 40
            httpGet:
              path: /actuator/health/liveness
              port: 8080
          readinessProbe:
            initialDelaySeconds: 20
            periodSeconds: 30
            httpGet:
              path: /actuator/health/readiness
              port: 8080

#
# see: https://kubernetes.io/docs/concepts/services-networking/_print/#defining-a-service
#
#
